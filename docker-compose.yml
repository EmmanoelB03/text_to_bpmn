services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      # Volume para não perder os modelos (Llama 3, Mistral, etc.) ao reiniciar
      - ollama_storage:/root/.ollama


    restart: always

volumes:
  ollama_storage: